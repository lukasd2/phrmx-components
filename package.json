{
    "name": "root",
    "private": true,
    "devDependencies": {
        "lerna": "^3.22.1"
    },
    "scripts": {
        "start": "lerna run start --scope=query-ui",
        "test": "lerna run test"
    }
}
